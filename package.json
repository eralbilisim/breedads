{
  "name": "breedads",
  "version": "1.0.0",
  "description": "BreedAds - AI-Powered Ad Management Platform for Meta & Google Ads",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "db:migrate": "cd server && npx prisma migrate deploy",
    "db:generate": "cd server && npx prisma generate",
    "postinstall": "cd server && npm install && npx prisma generate && cd ../client && npm install && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
